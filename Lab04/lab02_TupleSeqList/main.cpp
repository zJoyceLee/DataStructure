//STL
#include <iostream>
#include <vector>
#include <list>
//Current Project
#include "Triple.hpp"

void printMatrix(const std::vector<Triple<int>> & myvec) {
    int k = 0;//myvec[k] -> myvec[now]
    for(int i = 0; i < row; ++i) {
        for(int j = 0; j < col; ++j) {
            //if(myvec[now].row == i && myvec[now].col == j) --> print(myvec[now].value);else --> print("0")
            //if(myvec[k].)
        }
    }
}
//this.row == rhs.row && this.col == rhs.col
